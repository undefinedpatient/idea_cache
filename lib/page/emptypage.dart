import 'package:flutter/material.dart';

class ICEmptyPage extends StatelessWidget {
  const ICEmptyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("No Content"));
  }
}
